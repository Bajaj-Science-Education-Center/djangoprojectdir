from django.core.files.storage import default_storage
import os
import sys

from datetime import *

#class Util():
    #def addFacultyImage(self, image, name):
    #    formatted_date = '_'+str(datetime.now().day)+'_'+str(datetime.now().month)+'_'+str(datetime.now().year)+'_'+str(datetime.now().hour)+'_'+str(datetime.now().minute)+'_'+str(datetime.now().second)
    #    file_name = formatted_date + '.png'
    #    file = default_storage.open(file_name, 'w')
    #    file.write(image)
    #    file.close()
    #    path = file
    #
    #    return(path)
